---
development:
  adapter: postgresql
  url: "postgresql://localhost:5432/libreverse_development"
  channel_prefix: libreverse_development

test:
  adapter: test

production:
  adapter: postgresql
  url: "postgresql://localhost:5432/libreverse_production"
  channel_prefix: libreverse_production
