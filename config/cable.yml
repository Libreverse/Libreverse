# AnyCable configuration with Redis pub/sub backend
# AnyCable-Go handles WebSocket connections, Rails handles broadcasting

development:
    adapter: any_cable
    channel_prefix: libreverse_development

test:
    adapter: any_cable
    channel_prefix: libreverse_test

production:
    adapter: any_cable
    channel_prefix: libreverse_production
