# TiDB configuration for Libreverse
# TiDB is MySQL-compatible distributed database

# Common pool size calculation
<%
  require 'etc'
    nproc = Etc.respond_to?(:nprocessors) ? Etc.nprocessors : 5
    # Prefer thread budget values when present; otherwise fall back to legacy heuristics
    pool_size = Integer(ENV.fetch('APP_THREADS_BUDGET', '0'))
    if pool_size <= 0
        pool_size = [(nproc / 2) * 2 - 4, 5].max
    end
    # SQLite threads derived from budget if present, else fallback
    sqlite_threads = Integer(ENV.fetch('SQLITE_THREADS_BUDGET', '0'))
    if sqlite_threads <= 0
        sqlite_threads = [nproc, 2].max
    end
%>

default: &default
    adapter: trilogy
    encoding: utf8mb4
    collation: utf8mb4_unicode_ci
    pool: <%= pool_size %>
    reconnect: true
    checkout_timeout: 5
    # TiDB works well with these MySQL variables
    variables:
        sql_mode: 'STRICT_TRANS_TABLES,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION'
        # TiDB handles distributed transactions well
        transaction_isolation: 'READ-COMMITTED'

development:
    primary:
        <<: *default
        database: libreverse_development
        username: root
        password: 
        host: 127.0.0.1
        port: 4000
    queue:
        <<: *default
        database: libreverse_development
        username: root
        password: 
        host: 127.0.0.1
        port: 4000
        migrations_paths: db/queue_migrate
    cache:
        adapter: sqlite3
        encoding: utf8
        database: db/cache_development.sqlite3
        pool: <%= pool_size %>
        migrations_paths: db/cache_migrate
        pragmas:
            cache_size: -20000
            cache_spill: 0
            cell_size_check: 0
            checkpoint_fullfsync: 0
            foreign_keys: 0
            fullfsync: 0
            journal_mode: 'WAL'
            journal_size_limit: 1000000
            wal_checkpoint: 'PASSIVE'
            locking_mode: 'NORMAL'
            mmap_size: 268435456
            page_size: 4096
            secure_delete: 'ON'
            synchronous: 0
            temp_store: 2
            threads: <%= sqlite_threads %>
            incremental_vacuum: 0
            read_uncommitted: 1
            trusted_schema: 'OFF'
            wal_autocheckpoint: 1000
            auto_vacuum: 'NONE'

test:
    primary:
    <<: *default
    database: libreverse_test
    username: <%= ENV["TIDB_USERNAME"] %>
    password: <%= ENV["TIDB_PASSWORD"] %>
    host: <%= ENV["TIDB_HOST"] %>
    port: 4000
    ssl_mode: VERIFY_IDENTITY
    sslca: <%= (RUBY_PLATFORM =~ /darwin/ ? "/etc/ssl/cert.pem" : "/etc/ssl/certs/ca-certificates.crt") %>
    cache:
        adapter: sqlite3
        encoding: utf8
        database: db/cache_test.sqlite3
        pool: <%= pool_size %>
        migrations_paths: db/cache_migrate
        pragmas:
            cache_size: -20000
            cache_spill: 0
            cell_size_check: 0
            checkpoint_fullfsync: 0
            foreign_keys: 0
            fullfsync: 0
            journal_mode: 'WAL'
            journal_size_limit: 1000000
            wal_checkpoint: 'PASSIVE'
            locking_mode: 'NORMAL'
            mmap_size: 268435456
            page_size: 4096
            secure_delete: 'ON'
            synchronous: 0
            temp_store: 2
            threads: <%= sqlite_threads %>
            incremental_vacuum: 0
            read_uncommitted: 1
            trusted_schema: 'OFF'
            wal_autocheckpoint: 1000
            auto_vacuum: 'NONE'

production:
    primary:
        <<: *default
        database: libreverse_production
        username: <%= ENV["TIDB_USERNAME"] %>
        password: <%= ENV["TIDB_PASSWORD"] %>
        host: <%= ENV["TIDB_HOST"] %>
        port: 4000
        ssl_mode: VERIFY_IDENTITY
        sslca: <%= (RUBY_PLATFORM =~ /darwin/ ? "/etc/ssl/cert.pem" : "/etc/ssl/certs/ca-certificates.crt") %>
    queue:
        <<: *default
        database: libreverse_production
        username: <%= ENV["TIDB_USERNAME"] %>
        password: <%= ENV["TIDB_PASSWORD"] %>
        host: <%= ENV["TIDB_HOST"] %>
        port: 4000
        ssl_mode: VERIFY_IDENTITY
        sslca: /etc/ssl/certs/ca-certificates.crt
        migrations_paths: db/queue_migrate
    cache:
        adapter: sqlite3
        encoding: utf8
        database: db/cache_production.sqlite3
        pool: <%= pool_size %>
        migrations_paths: db/cache_migrate
        pragmas:
            cache_size: -20000
            cache_spill: 0
            cell_size_check: 0
            checkpoint_fullfsync: 0
            foreign_keys: 0
            fullfsync: 0
            journal_mode: 'WAL'
            journal_size_limit: 1000000
            wal_checkpoint: 'PASSIVE'
            locking_mode: 'NORMAL'
            mmap_size: 268435456
            page_size: 4096
            secure_delete: 'ON'
            synchronous: 0
            temp_store: 2
            threads: <%= sqlite_threads %>
            incremental_vacuum: 0
            read_uncommitted: 1
            trusted_schema: 'OFF'
            wal_autocheckpoint: 1000
            auto_vacuum: 'NONE'
    cable:
        <<: *default
        database: libreverse_production
        username: <%= ENV["TIDB_USERNAME"] %>
        password: <%= ENV["TIDB_PASSWORD"] %>
        host: <%= ENV["TIDB_HOST"] %>
        port: 4000
        ssl_mode: VERIFY_IDENTITY
        sslca: <%= (RUBY_PLATFORM =~ /darwin/ ? "/etc/ssl/cert.pem" : "/etc/ssl/certs/ca-certificates.crt") %>
        migrations_paths: db/cable_migrate