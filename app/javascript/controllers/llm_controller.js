// LLM feature stubbed out; intentionally empty to avoid bundling large assets.
import { Controller } from "@hotwired/stimulus";

export default class extends Controller {
    connect() {
        // Placeholder until new LLM frontend is added.
    }
}
