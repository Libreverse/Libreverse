!!!
%html{dir: "ltr", lang: "en-GB"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %link{as: "font", crossorigin: "", href: vite_asset_path("~/fonts/Lazer84.woff2").to_s, rel: "preload", type: "font/woff2"}/
    %link{as: "font", crossorigin: "", href: vite_asset_path("~/fonts/Hack-Regular.woff2").to_s, rel: "preload", type: "font/woff2"}/
    %link{as: "font", crossorigin: "", href: vite_asset_path("~/fonts/Hack-Italic.woff2").to_s, rel: "preload", type: "font/woff2"}/
    - if Rails.env.development?
      = vite_stylesheet_tag "javascript/application.js"
      = vite_javascript_tag "javascript/application.js"
    - else
      = inline_vite_stylesheet "~/javascript/application.js"
      = inline_vite_javascript "~/javascript/application.js"
      = inline_vite_legacy_javascript "~/javascript/application.js"
    = render "layouts/development_headers" if Rails.env.development?
    = render "layouts/meta_tags"
    /
      Some decently important security stuff, just matters that it's present,
      so we don't need to bother search engines by putting it at the top
    = csrf_meta_tags
    / Vite HMR in development. Does not result in anything in prod
    = vite_client_tag
  %body
    .underconstruction
      %span
        ⚠️👨‍🔧 This is the v1 alpha release of Libreverse. Expect bad code,
        crashes, performance issues and downtime. 👨‍🔧⚠️
    %noscript
      .noscript-message
        .noscript-message__content
          %h1.noscript-message__heading JavaScript Required
          %p.noscript-message__text
            This application requires JavaScript to function properly. Please
            enable JavaScript in your browser settings and refresh the page.
    = render "layouts/flash_messages"
    #toast-container
    %main.content-wrapper
      = yield
    = render partial: "layouts/drawer" if page_with_drawer?
