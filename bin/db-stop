#!/usr/bin/env bash
echo "Stopping local databases..."

echo "Note: TiDB and Valkey are running as background services."
echo "To stop them, run:"
echo "  launchctl unload ~/Library/LaunchAgents/com.pingcap.tidb.plist"
echo "  brew services stop valkey"