#!/bin/bash
set -e
# Initial build after delay
bin/shakapacker
# Watch JSX and TSX files for changes and rebuild
fswatch -r app/javascript --include '\.jsx?$' --include '\.tsx?$' | while read; do
    bin/shakapacker
done