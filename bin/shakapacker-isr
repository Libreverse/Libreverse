#!/bin/bash
set -e
# Initial build after delay
sleep 10
bin/shakapacker
# Watch JSX files for changes and rebuild
fswatch -r app/javascript --include '\.jsx$' | while read; do
    bin/shakapacker > /dev/null 2>&1
done